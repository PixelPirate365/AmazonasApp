export default class ShippingModel {
    constructor(fullName, address, city, postalCode, country) {
        this.fullName = fullName;
        this.address = address;
        this.city = city;
        this.postalCode = postalCode;
        this.country = country;
    }
}